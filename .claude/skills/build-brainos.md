Run `npm run build` inside `brain-os-mcp/` and report the result.

- If the build succeeds with no output: respond "Build clean."
- If there are TypeScript errors: show the exact error lines and suggest a targeted fix.
- Do not explain what TypeScript compilation is — just show the result and act on failures.
